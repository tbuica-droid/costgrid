{
  "name": "costgrid",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build --force",
    "gateway": "node --import tsx packages/gateway/src/main.ts",
    "gateway:dev": "node --import tsx --watch packages/gateway/src/main.ts",
    "verify-pricing": "npm run build --silent && node scripts/verify-pricing.mjs",
    "sync-design-tokens": "node scripts/sync-design-tokens.mjs",
    "check-design-tokens": "node scripts/sync-design-tokens.mjs --check"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11"
  }
}
